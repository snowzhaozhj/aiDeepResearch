---
interface Props {
  headers: string[];
  rows: Record<string, string>[];
  highlight?: string;
}

const { headers, rows, highlight } = Astro.props;

function renderCell(text: string): string {
  return text.replace(
    /\[([^\]]+)\]\(([^)]+)\)/g,
    '<a href="$2" target="_blank" rel="noopener">$1</a>'
  );
}
---

<div class="matrix-wrap">
  <div class="matrix-scroll">
    <table class="matrix">
      <thead>
        <tr>
          {headers.map((h) => (
            <th>{h}</th>
          ))}
        </tr>
      </thead>
      <tbody>
        {rows.map((row) => (
          <tr class={highlight && row[headers[0]] === highlight ? "row-highlight" : ""}>
            {headers.map((h, i) => (
              <td class={i === 0 ? "cell-name" : ""} set:html={renderCell(row[h])} />
            ))}
          </tr>
        ))}
      </tbody>
    </table>
  </div>
</div>

<style>
  .matrix-wrap {
    margin: 1.8rem 0;
    max-width: 96ch;
  }

  .matrix-scroll {
    overflow-x: auto;
    border-radius: var(--r-lg);
    border: 1px solid var(--border);
  }

  .matrix {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.875rem;
  }

  .matrix th {
    text-align: left;
    padding: 0.8rem 1rem;
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    color: var(--muted);
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    white-space: nowrap;
  }

  .matrix td {
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--border-dim);
    vertical-align: top;
    color: var(--ink-2);
    line-height: 1.5;
  }

  .matrix tbody tr:last-child td {
    border-bottom: none;
  }

  .matrix tbody tr:hover td {
    background: var(--surface);
  }

  .cell-name {
    font-weight: 600;
    color: var(--ink);
    white-space: nowrap;
  }

  .matrix :global(a) {
    color: var(--primary);
    text-decoration: underline;
    text-decoration-color: oklch(from var(--primary) l c h / 0.3);
    text-underline-offset: 2px;
    transition: text-decoration-color 150ms var(--ease);
  }
  .matrix :global(a:hover) {
    text-decoration-color: var(--primary);
  }

  .row-highlight td {
    background: var(--primary-dim) !important;
  }
  .row-highlight .cell-name {
    color: var(--primary);
  }
</style>
